PROGRAM test
BEGIN

	INT i,a,num;
	
	FUNCTION VOID main()
	BEGIN
		num := 7;
		i := 2;	
		a := 42;
	
		IF (i<5)
			num := num +a;
			IF ( i < 3)
				num := num + a;
			ENDIF
		ELSIF (i >= 5)
			num := num - a;
		ENDIF

		WRITE(num);
	END
END
